#include<stdio.h>
int main()
{
    char n[15];
    int i;
    scanf("%s",n);
    for(i=0;i<n[i];i++)
    {
        switch(n[i])
        {
            case '0': printf("Zero"); break;
            case '1': printf("One"); break;
            case '2': printf("Two");

    }

    return 0;
}
